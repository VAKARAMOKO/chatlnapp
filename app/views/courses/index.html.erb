<section class="section">
 <div class="container">
    <div class="columns">
    <% if user_signed_in?%>
            
        
        <%= render 'matieres' %>   
        <%= render 'feed' %>
        <%= render 'who-to-follow' %>
        
    <% else %>
      <%= render template: 'home/index' %>
    <% end %>      
    </div>
  </div>
</section>
